apiVersion: v1
kind: Service
metadata:
   name: node-service
   labels:
     app: node-service
      
spec:
   type: LoadBalancer
   ports:
     - protocol: TCP
       port: 8059
       targetPort: 8059
   selector:
     app: sonede-backend-deployment